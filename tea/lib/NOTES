# cli
  # typing "./bin/teahead" bgins a tea session
  # asks for your name and welcomes user
  # gives a menu to choose from
      # choose from a list
          # list all teas a-z
          # list teas by type
              # choose from sub-list
          # find by name
          # search by phrase or word
    # eventually print a list of teas based on input
    # select a tea profile by index number
    # imports profile based on input
    # prints tea profile
    # clear navigation instructions

     #    ./bin/teahead
     #    ./bin/console

     #    git commit -a -m "updated notes"
     #    git push origin master
     #    ashley3schultz
